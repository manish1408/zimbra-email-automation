# Copy to deploy.env (gitignored) for remote server commands.
# Used by scripts/remote-mail-poller.sh and scripts/redeploy.sh

DEPLOY_HOST=176.123.3.2
DEPLOY_USER=root

# Optional — omit when using SSH keys (recommended)
# DEPLOY_PASSWORD=

# systemd unit name on the server
MAIL_POLLER_SERVICE=zimbra-mail-poller
